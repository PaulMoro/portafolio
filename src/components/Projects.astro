---
// Projects section component
const projects = [
  {
    title: "Tienda Colombia Tech Week",
    description: "Tienda merch para el evento de tecnologia mas importante de colombia sobre tecnologia, innovacion y emprendimiento",
    technologies: ["Shopify", "Liquid", "JavaScript", "CSS"],
    image: "/ctw.png",
    link: "colombiatechweek.store/",
    featured: true
  },
  {
    title: "Calculadora Tuplata.com",
    description: "Desarrollo en un proyecto Wordpress para la calculadora que determinaria los prestamos del producto, inicio del funnel del usuario y conexion con otros pluggins para el manejo del usuario.",
    technologies: ["WordPress", "JavaScript", "CSS"],
    image: "/project.png",
    link: "github.com/PaulMoro/tuPlataApp"
  },
  {
    title: "LaraMusic",
    description: "Aplicación web de musica conectada con Spotify, para escuchar musica y ver los artistas, canciones y playlists.",
    technologies: ["React", "Sass", "Next"],
    image: "/project.png",
    link: "github.com/LaraMusic"
  },
  {
    title: "E-collect",
    description: "Landing para el evento JAM (Hackathon) de donde salimos ganadores, la idea era hacer una bolsa de valores para la basura generada en cada casa y las personas poder ver el valor que tenia y venderla si asi lo deseaban",
    technologies: ["Gastby", "Netlify", "AWS"],
    image: "/E-collect.png",
    link: "E-collect.netlify.app/"
  }
];
---

<section id="projects" class="py-20 bg-black">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
        Mis <span class="bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent">Proyectos</span>
      </h2>
      <p class="text-xl text-gray-400 max-w-3xl mx-auto">
        Estos son solo un piñado de mis proyectos, todos son de mi autoría, ya que la mayoria de mi trabajo ha sido para empresas privadas, por lo que no puedo mostrarlos aquí.
      </p>
    </div>
    
    <!-- Featured Project -->
    {projects.filter(p => p.featured).map((project) => (
      <div class="mb-16">
        <div class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-gray-900 to-gray-800 border border-gray-700">
          <div class="grid lg:grid-cols-2 gap-8 items-center">
            <!-- Project Image -->
            <div class="relative h-80 lg:h-full">
              <div class="absolute inset-0 bg-gradient-to-br from-blue-500/20 to-purple-500/20 rounded-l-2xl"></div>
              <img 
                src={project.image} 
                alt={project.title}
                class="w-full h-full object-cover rounded-l-2xl"
              />
              <div class="absolute top-4 left-4">
                <span class="px-3 py-1 bg-blue-500/90 text-white text-sm font-medium rounded-full">
                  Proyecto Destacado
                </span>
              </div>
            </div>
            
            <!-- Project Content -->
            <div class="p-8 lg:p-12">
              <div class="space-y-6">
                <div>
                  <h3 class="text-3xl font-bold text-white mb-4">{project.title}</h3>
                  <p class="text-lg text-gray-300 leading-relaxed">{project.description}</p>
                </div>
                
                <div class="space-y-4">
                  <h4 class="text-lg font-semibold text-white">Tecnologías utilizadas</h4>
                  <div class="flex flex-wrap gap-2">
                    {project.technologies.map((tech) => (
                      <span class="px-3 py-1 bg-blue-500/20 text-blue-400 rounded-full text-sm font-medium border border-blue-500/30">
                        {tech}
                      </span>
                    ))}
                  </div>
                </div>
                
                <div class="pt-4">
                  <a 
                    href={project.link}
                    class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-blue-500 to-purple-600 text-white font-semibold rounded-lg hover:from-blue-600 hover:to-purple-700 transition-all duration-300 transform hover:scale-105"
                  >
                    Ver Proyecto
                    <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
                    </svg>
                  </a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    ))}
    
    <!-- Other Projects Grid -->
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {projects.filter(p => !p.featured).map((project) => (
        <div class="group relative overflow-hidden rounded-xl bg-gradient-to-br from-gray-900 to-gray-800 border border-gray-700 hover:border-gray-600 transition-all duration-300 transform hover:scale-105">
          <!-- Project Image -->
          <div class="h-48 bg-gradient-to-br from-gray-800 to-gray-700 overflow-hidden">
            <img 
              src={project.image} 
              alt={project.title}
              class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-300"
            />
          </div>
          
          <!-- Project Content -->
          <div class="p-6">
            <h3 class="text-xl font-bold text-white mb-3 group-hover:text-blue-400 transition-colors">
              {project.title}
            </h3>
            <p class="text-gray-400 mb-4 leading-relaxed">
              {project.description}
            </p>
            
            <!-- Technologies -->
            <div class="flex flex-wrap gap-2 mb-4">
              {project.technologies.slice(0, 3).map((tech) => (
                <span class="px-2 py-1 bg-gray-800 text-gray-300 rounded text-xs font-medium">
                  {tech}
                </span>
              ))}
              {project.technologies.length > 3 && (
                <span class="px-2 py-1 bg-gray-800 text-gray-300 rounded text-xs font-medium">
                  +{project.technologies.length - 3}
                </span>
              )}
            </div>
            
            <!-- Link -->
            <a 
              href={project.link}
              class="inline-flex items-center text-blue-400 hover:text-blue-300 font-medium text-sm transition-colors"
            >
              Ver proyecto
              <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
              </svg>
            </a>
          </div>
        </div>
      ))}
    </div>
    
    <!-- CTA -->
    <div class="text-center mt-16">
      <p class="text-gray-400 mb-6">¿Interesado en trabajar juntos?</p>
      <a 
        href="#contact"
        class="inline-flex items-center px-8 py-4 bg-gradient-to-r from-blue-500 to-purple-600 text-white font-semibold rounded-lg hover:from-blue-600 hover:to-purple-700 transition-all duration-300 transform hover:scale-105"
      >
        Hablemos de tu proyecto
      </a>
    </div>
  </div>
</section>
